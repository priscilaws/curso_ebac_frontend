function multiplicar(a: number, b: number): number {
    return a * b;
}

function dizOi(nome: string): string {
    return "Oi" + nome + "!";
}

const resultado = multiplicar(3, 20);
console.log(resultado);

const saudar = dizOi("John");
console.log(saudar);
